long int monad(char input[14]) {
  long int x = 0, y = 0, z = 0, w = 0;

  w = input[0];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 14;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 14;
  y *= x;
  z += y;
  w = input[1];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 14;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 2;
  y *= x;
  z += y;
  w = input[2];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 14;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 1;
  y *= x;
  z += y;
  w = input[3];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 12;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 13;
  y *= x;
  z += y;
  w = input[4];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 15;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 5;
  y *= x;
  z += y;
  w = input[5];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -12;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 5;
  y *= x;
  z += y;
  w = input[6];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -12;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 5;
  y *= x;
  z += y;
  w = input[7];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 12;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 9;
  y *= x;
  z += y;
  w = input[8];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -7;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 3;
  y *= x;
  z += y;
  w = input[9];
  x *= 0;
  x += z;
  x %= 26;
  z /= 1;
  x += 13;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 13;
  y *= x;
  z += y;
  w = input[10];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -8;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 2;
  y *= x;
  z += y;
  w = input[11];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -5;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 1;
  y *= x;
  z += y;
  w = input[12];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -10;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 11;
  y *= x;
  z += y;
  w = input[13];
  x *= 0;
  x += z;
  x %= 26;
  z /= 26;
  x += -7;
  x = x == w;
  x = x == 0;
  y *= 0;
  y += 25;
  y *= x;
  y += 1;
  z *= y;
  y *= 0;
  y += w;
  y += 8;
  y *= x;
  z += y;
  return z;
}
